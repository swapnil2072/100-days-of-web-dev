<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head') %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
    <!-- to include other html file into it just like inline functions -->
    <%- include('includes/header') %> <%- include('includes/side-drawer') %>
    <main>
      <h1>Recommended restaurants</h1>
      <!-- using if statement to render conditional content using conditional ejs -->
      <% if(numberOfRestaurants===0) { %>
      <p>
        Unfortunately,we have no restaurant yet - maybe start recommending some?
      </p>
      <% } else { %>
      <p>Find your next favorite restaurants with help of our other users!</p>
      <p>We Found <%= numberOfRestaurants %> restaurants</p>
      <ul id="restaurants-list">
        <!-- using for loop to render all restaurant which are present in json file  -->

        <% for (const restaurant of restaurants) { %> <%-
        include('includes/restaurant/restaurant-item',{restaurant:restaurant})
        %> <% } %>
      </ul>
      <% } %>
    </main>
  </body>
</html>
